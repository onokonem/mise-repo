.claude/
draft.txt

# Provider build tree — generated, never committed.
# The published source of truth is .easyp/manifest.json (regenerated by the
# publish task) plus the GitHub Release assets. artifacts/ holds local mirrors
# and build output only.
artifacts/
.cache/
*.tmp

.DS_Store

# consumer build output
/consumer/consumer
/.mise/build-tools
